@{
    var current = ViewContext.RouteData.Values["controller"]?.ToString();
}

<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="utf-8"/>
    <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
    <title>@ViewData["Title"] - L'Escapade Ardennaise</title>

    <link rel="stylesheet" href="~/lib/bootstrap/dist/css/bootstrap.min.css"/>
    <link rel="stylesheet" href="~/css/site.css" asp-append-version="true"/>
</head>

<body class="min-vh-100 d-flex flex-column">
<header>
    <nav class="navbar navbar-expand-sm py-3">
        <div class="container-fluid px-4 px-md-5">
            
            <a class="navbar-brand text-dark fw-bolder text-uppercase brand-logo m-0" asp-area="" asp-controller="Home" asp-action="Index">
                L'ESCAPADE ARDENNAISE
            </a>

            <button class="navbar-toggler border-0 shadow-none" type="button" data-bs-toggle="collapse" data-bs-target="#mainNavbar" aria-controls="mainNavbar"
                    aria-expanded="false" aria-label="Toggle navigation">
                <span class="navbar-toggler-icon"></span>
            </button>

            <div class="collapse navbar-collapse" id="mainNavbar">
                <ul class="navbar-nav gap-3 gap-sm-4 mx-auto my-3 my-sm-0 text-center">
                    <li class="nav-item">
                        <a class="nav-link text-dark text-uppercase nav-item-link px-2 @(current == "Home" ? "active-link" : "")" asp-area="" asp-controller="Home" asp-action="Index">HOME</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link text-dark text-uppercase nav-item-link px-2 @(current == "Location" ? "active-link" : "")" asp-area="" asp-controller="Location" asp-action="Index">LOCATION</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link text-dark text-uppercase nav-item-link px-2 @(current == "Booking" ? "active-link" : "")" asp-area="" asp-controller="Booking" asp-action="NewBooking">BOOKING</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link text-dark text-uppercase nav-item-link px-2 @(current == "Contact" ? "active-link" : "")" asp-area="" asp-controller="Contact" asp-action="Index">CONTACT</a>
                    </li>
                </ul>
            </div>

        </div>
    </nav>
</header>

<div class="container">
    <main role="main" class="pb-3">
        @RenderBody()
    </main>
</div>

<footer class="border-top border-dark footer text-muted">
    <div class="container mt-5">
        L'Escapade Ardennaise 
        - <a asp-area="" asp-controller="Contact" asp-action="Index" class="text-dark fw-bold">Contact</a>
        - <a asp-area="" asp-controller="Admin" asp-action="Login" class="text-dark fw-bold">Login</a>
    </div>
</footer>

<script src="~/lib/jquery/dist/jquery.min.js"></script>
<script src="~/lib/bootstrap/dist/js/bootstrap.bundle.min.js"></script>
<script src="~/js/site.js" asp-append-version="true"></script>
@await RenderSectionAsync("Scripts", required: false)
</body>
</html>